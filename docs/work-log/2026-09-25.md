# Work Log --- 2026-09-25

## Focus

Investigated the configuration behind OSHA ID-110 QCSM recovery before recycling the historical validation dataset.

## Completed

- Traced `ISE / 1280 F/T` through the LabWare configuration UI.
- Inspected the full `CALC_QC_CONC` subroutine.
- Confirmed F/T is a unitless Found/Theoretical recovery ratio.
- Confirmed the 1280 F/T variable mappings for `found`, `theory`, and `theoryElement`.
- Compared the 1280 recovery pattern with an established GC `MeCl2 F/T` implementation using the same `CALC_QC_CONC` routine.
- Inspected `CALC_GC_FINAL` and confirmed explicit sample-type branching, including QCSM/RLSM behavior that retains mass in `UG`.
- Compared ISE Air Volume with GC/IC shared `CALC_AIR_VOL` behavior.
- Compared current DEV calculation source with historical environment `OLD_DEV_050526`.
- Found historical QCSM branching in `1280 (Final)` that was absent from current DEV.
- Confirmed no analogous historical branch in `1460 (Final)` in that environment.
- Compared parent ISE Analysis Variation records between environments; no substantive ID-110 differences were found.

## DEV configuration changes pending validation

- ISE Air Volume changed to call shared `CALC_AIR_VOL`.
- Historical QCSM branch restored to `1280 (Final)` with provenance comments.
- Analogous QCSM branch added to `1460 (Final)` with provenance comments.

These changes are not yet validated. The next action is a fresh regression execution of the historical reference dataset with both QCSM035 and QCSM036 included.

## Important open item

The last confirmed state of `1460 F/T` was blank calculation source with no calculation variables. Before the 1460 QCSM recovery issue is considered resolved, the current configuration of `1460 F/T` must be explicitly verified and its recovery behavior demonstrated in the next run.
